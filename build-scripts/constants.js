const DIRS_TO_COPY = ['assets', 'views', 'styles'];
const SCRIPTS_DIR = 'scripts';
const DEST_DIR = 'dist';
const SOURCE_DIR = 'src';
const RELOAD_DELAY = 500;

module.exports = {
    DIRS_TO_COPY,
    SCRIPTS_DIR,
    DEST_DIR,
    SOURCE_DIR,
    RELOAD_DELAY,
};
